# per-machine config (generated by scripts/setup.ps1) — never commit absolute local paths
config/env.json

# node deps (installed locally by setup.ps1)
node_modules/

# generated artifacts
*.pptx
*.pdf
*.tmp
/render/
/scratch/
# screenshots from shoot.ps1 / renders (keep curated docs/ images instead)
*_预览.png
selftest-*.jpg
